{
  "name": "esercitazione-mariadb",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-mssql.mssql",
        "angular.ng-template",
        "dbaeumer.vscode-eslint"
      ]
    }
  },
  "postCreateCommand": "sudo apt update && sudo apt install mariadb-server python3 python3-pip -y && sudo service mariadb start && sudo npm install -g @angular/cli",
  "portsAttributes": {
    "4200": {

      "label": "Angular Dev Server",
      "onAutoForward": "notify"
    }
  },
  "forwardPorts": [4200]
}
